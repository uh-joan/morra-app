# dependencies / build
node_modules/
dist/

# large model binaries (re-fetch via spikes/models/fetch-*.sh)
spikes/models/*.zip

# PRIVATE: user voice recordings — never commit (corpus governance)
spikes/corpus-jani/
*.wav

# art pipeline artifacts
spikes/s05-art/out/
spikes/s05-art/final/
spikes/s05-art/character.blend*

# OS noise
.DS_Store

# live session logs (local only)
spikes/logs/

# OMC session machinery + local agent/tool state — never project content.
# Whole-tree: plans, specs, sessions, project-memory, state, logs, at the
# root and nested under any package (audit M8/A4 wants this out of git).
.omc/
**/.omc/

# Claude Code local tooling (preview launch config, session state)
.claude/
